# Credentials — NEVER commit these
.telegram-config
.mcp.json
jira.json

# Personal / local instructions — read by Claude but never committed.
# Public users: drop your own private context here; it is gitignored.
CLAUDE.local.md
.claude/settings.local.json

# Runtime state files
.telegram-state.json
.telegram-task-state.json
.telegram-task-daemon.lock

# Conversation archive (personal chat history + per-day summaries)
archive/

# Daemon logs
mcp/task-daemon.log
mcp/task-daemon.log.err

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.claude-queue.json
*.tmp